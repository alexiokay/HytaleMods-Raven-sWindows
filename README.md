# Raven's Windows

A Hytale mod that adds connected glass windows that auto-connect like fences. Includes various window styles with 12 shape variants (Single, Horizontal, Vertical, Corners, T-shapes, Cross).
